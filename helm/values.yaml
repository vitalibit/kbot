# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  registry: ghcr.io
  repository: vitalibit/kbot
  tag: "v1.0.8-e666721"
  os: linux
  arch: amd64
secret:
  name: kbot
  key: token
  env: TELE_TOKEN
